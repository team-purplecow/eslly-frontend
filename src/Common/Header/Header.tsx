import Image from 'next/image';
import { HeaderContainer } from './Header.styled';

export const Header = () => {
  return (
    <HeaderContainer>
      <Image src={''} alt={''} />
    </HeaderContainer>
  );
};
