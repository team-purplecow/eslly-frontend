import { HeaderContainer } from './Header.styled';

export const Header = () => {
  return (
    <HeaderContainer>
      {/* <LogoBox src={} alt={''} /> */}
      here is header
    </HeaderContainer>
  );
};
