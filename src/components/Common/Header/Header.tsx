import { HeaderContainer, LogoBox } from './Header.styled';
import tempLogo from 'Assets/';

export const Header = () => {
  return (
    <HeaderContainer>
      <LogoBox src={} alt={''} />
    </HeaderContainer>
  );
};
